<% layout('layout') -%>

<% books.forEach(function(book) { %>
  <div class="book">
    <h3 id="bookname"><%= book.title %></h3>
    <p><%= book.description || 'No description available' %></p>
  </div>
<% }) %>
